<%= form_with url: login_path, local: true do |f| %>
  <div class="max-w-md mx-auto mt-16 p-8 bg-white rounded-lg shadow">
    <h1 class="text-3xl font-bold text-purple-700 mb-8 text-center tracking-widest">ログイン</h1>
    <div class="mb-6">
      <%= f.label :email, class: "block text-lg font-semibold mb-2 text-gray-700" %>
      <%= f.email_field :email, class: "form-control w-full px-4 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-purple-300" %>
    </div>
    <div class="mb-8">
      <%= f.label :password, class: "block text-lg font-semibold mb-2 text-gray-700" %>
      <%= f.password_field :password, class: "form-control w-full px-4 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-purple-300" %>
    </div>
    <!--現在の仕様ではログインボタンを押すとquestionのindex画面へ遷移されるように設計しているが、なぜか表示されない、おそらく「turbostream」の操作によるものであると思われる-->
    <%= f.submit "ログイン", class: "w-full bg-purple-500 hover:bg-purple-600 text-white text-xl font-bold py-3 rounded-full transition" %>
  </div>
<% end %>