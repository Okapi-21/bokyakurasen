<%= link_to start_question_path(parent), class: "block bg-white rounded-xl shadow-lg p-6 flex flex-col items-center hover:bg-purple-50 transition border border-purple-100" do %>
  <span class="text-lg font-semibold text-gray-800 mb-4 text-center"><%= parent.title %></span>
  <% if current_user.own?(parent) %>
    <div class="ms-auto">
        <%= link_to "編集", edit_question_path(parent), class: "" %>
    </div>
  <% end %> 
<% end %>